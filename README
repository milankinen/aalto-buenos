Eclipse debug:

1) lataa ftp://sourceware.org/pub/gdb/releases/gdb-7.0.1a.tar.gz
2) sudo aptitude install libncurses5-dev
3) pura + sen jälkeen:
./configure --disable-werror --prefix=$PREFIX -with-mpc=$PREFIX --target=mipsisa32-elf --program-prefix=mips-elf-

RUNNING TESTS:

Kernel tests and solutions to toy problems are located in /kernel_tests

Running the solution for makewater

1)gmake all
2)start yamst
3)yams buenos makewater
